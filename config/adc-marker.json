{"_comment": "Deliberately not a valid credential. It exists only to satisfy pi's fileExists gate; the real token comes from the metadata broker. If any program actually tries to read this, it will fail JSON parsing and say so loudly -- that is the intent."}
